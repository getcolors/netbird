{:paths ["src/clj" "src/resources"]
 :deps {io.github.getcolors/colors-compute {:git/url "https://github.com/getcolors/colors-compute.git" :git/sha "3451a05e719b0ad6809f3c88b241a8c010b8f58b" :deps/root "green"}
        org.clojure/clojure       {:mvn/version "1.12.5"}
        io.github.getcolors/green {:git/url "https://github.com/getcolors/green.git"
                                   :git/sha "3f33f5d4dcce1f8d97a11b6972e13eb3f0b37654"}
        ;; The two pins move together. Green 3f33f5d is where the SDK reports a
        ;; tofu launch failure — a missing stage directory, a missing binary —
        ;; as exit 127 and so as its `tofu output failed:` step error carrying
        ;; :dir, the way red and blue always did. ONCE 38e3cd6 is where
        ;; `read-state`, in the `compute` namespace this package delegates to,
        ;; dropped its java.io.IOException workaround and trusts that step
        ;; error alone; it needs green 3f33f5d, or a fresh clone's create
        ;; crashes instead of reporting its credentials. Below 38e3cd6, 04f9623
        ;; added `compute` itself and bc06f2f moved the machine keypair into the
        ;; operator's ~/.ssh. The ONCE pin cannot go below any of them.
        io.github.getcolors/once  {:git/url "https://github.com/getcolors/once.git"
                                   :git/sha "a1fe1be7a427dd2e406ff7befd1c43a53e7c3618"
                                   :deps/root "green"}}
 :aliases {:dev {:extra-paths ["test/clj" "test/resources"]}
           :test {:extra-paths ["test/clj" "test/resources"]
                  :extra-deps {io.github.cognitect-labs/test-runner
                               {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
                  :exec-fn cognitect.test-runner.api/test}}}
